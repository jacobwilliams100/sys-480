network:
  version: 2
  renderer: networkd
  ethernets:
    ens33:
      dhcp4: false
      addresses: ["{{ lan_ip }}/{{ prefix }}"]
      gateway4: "{{ gateway }}"
      nameservers:
        addresses: ["{{ name_server }}", "{{ gateway }}"]
        search: ["{{ domain }}"]